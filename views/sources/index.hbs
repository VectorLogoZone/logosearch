{{> above}}
<div class="alert alert-info">{{{t "SOURCES_PAGE.INFO_BOX_HTML"}}}</div>
<div class="alert alert-info">
    Check out the <a class="alert-link" href="https://www.github.com/VectorLogoZone/logosearch#criteria">criteria</a>
    for sources, and the <a class="alert-link" href="/alternatives/index.html">alternatives</a>
    that did not make the cut!
</div>

<table class="table table-striped">
    <thead>
        <tr>
            <!--
            <th class="d-none d-md-table-cell">Name</th>
            <th>Source</th>
            <th class="text-center"># Logos</th>
            <th class="d-none d-md-table-cell">Last update</th>
            -->
        </tr>
    </thead>
    <tbody>
{{#each sources}}
        <tr>
            <td class="text-center">
                {{#if this.logo}}
                <a href="{{#if this.website}}{{this.website}}{{else}}{{this.url}}{{/if}}"><img alt="{{this.name}}" src="{{{this.logo}}}" style="height:2.0em;" title="{{this.name}}" /></a>
                {{/if}}
            </td>
            <td>
                <a class="ml-2" href="{{this.handle}}/index.html">{{this.name}}</a>
            </td>
            <td class="d-none d-md-table-cell">
                <a href="{{this.url}}"><img alt="{{this.provider}}" class="mr-2" src="{{{this.provider_icon}}}"
                    style="height:2.0em;" title="{{this.provider}}" />{{this.provider}}</a>
            </td>
            <td>
                <a href="{{this.handle}}/logos.html">
                    <div class="mt-2 badge badge-primary float-right">{{addCommas this.images.length}}</div>
                </a>
            </td>
            <!--
            <td class="d-none d-md-table-cell">{{#if this.website}}<a href="{{this.website}}/">{{this.name}}</a>{{else}}{{this.name}}{{/if}}</td>
            <td>
                <img alt="{{this.provider}}" src="{{{this.provider_icon}}}" style="height:18pt;" title="{{this.provider}}" />
                <a href="{{this.url}}">{{this.name}}</a></td>
            <td class="text-center"><a href="{{this.handle}}/logos.html">{{addCommas this.images.length}}</a></td>
            <td class="d-none d-md-table-cell"><a href="{{this.handle}}/">{{this.lastmodified}}</a></td>
            -->
        </tr>
{{/each}}
    </tbody>
    <!--
    <tfoot>
        <tr>
            <td>
            </td>
        </tr>
    </tfoot>
    -->
</table>

<p>
{{addCommas sources.length}} sources with {{addCommas total}} logos <!--LATER: translation -->
</p>

{{> below}}
